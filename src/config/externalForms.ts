/**
 * ASF external form URLs — single place to update links (OTA-eligible).
 *
 * AMA / Slido Q&A links are NOT here; they come from schedule metadata on the backend
 * (see scheduleFeatures.ts metadata docs + parseScheduleCardMetadata in scheduleMetadata.ts).
 */

/** Schedule programme → "Leave a Feedback" (session detail + cards). */
export const SESSION_FEEDBACK_FORM_URL =
"https://forms.gle/iKaz858GLXqNfMMg8";

/** Meetings → "Leave Feedback" on scheduled meeting detail. */
export const MEETING_FEEDBACK_FORM_URL =
  "https://forms.gle/iaWtbKvP1kjC4QAj7";

/** Menu → App Suggestions. */
export const APP_SUGGESTIONS_FORM_URL =
  "https://forms.gle/KaiSs3ttsmCMEF1B9";

/** Overall event feedback (wire when/where product needs it). */
export const EVENT_FEEDBACK_FORM_URL =
  "https://form.typeform.com/to/jbMsvNVi";
